import { ACLImage } from "../acl-image-data-model/acl-image.model";

export class ACLBanner {
    typeId:string;
    name:string;
    version:string;
    variantId:string;
    variant:string;
    stateId:string;
    state:string;
    image:ACLImage;
}